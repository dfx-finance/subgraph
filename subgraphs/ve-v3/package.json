{
  "name": "ve-v3",
  "license": "UNLICENSED",
  "version": "1.0.0",
  "author": "DFX",
  "scripts": {
    "build": "graph build",
    "codegen": "graph codegen",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ dfx-ve-v3",
    "create-local": "graph create --node http://localhost:8020/ dfx-ve-v3",
    "remove-local": "graph remove --node http://localhost:8020/ dfx-ve-v3",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dfx-ve-v3",
    "test": "graph test",
    "prepare:mainnet": "exit 0",
    "prepare:polygon": "exit 0",
    "prepare:arbitrum": "exit 0"
  },
  "dependencies": {},
  "devDependencies": {}
}
